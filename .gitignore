node_modules/
dist/
build/
*.exe
.env
config.json
cli/config.json
agent/config.json
*.log
.DS_Store

# Compiled agent binaries
agent/clawpeteer-agent
agent/clawpeteer-agent-*

# Embedded CA certs (sensitive, don't commit)
agent/certs/*.crt
agent/certs/*.pem

# Embedded config (sensitive, don't commit)
agent/buildcfg/config.json
